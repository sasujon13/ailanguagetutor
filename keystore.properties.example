# Copy to keystore.properties (gitignored) for Play / production release signing.
# storeFile is relative to the project root.
storeFile=release-upload.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
